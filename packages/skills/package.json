{
  "name": "@resolve/skills",
  "private": true,
  "type": "module",
  "exports": "./src/index.ts",
  "scripts": {
    "build": "pnpm typecheck",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@resolve/contracts": "workspace:*",
    "@resolve/skill-sdk": "workspace:*",
    "zod": "^4.4.3"
  }
}
